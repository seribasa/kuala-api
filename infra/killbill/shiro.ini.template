[users]
# Format: username = password, role1, role2, ...
# Default admin user with root permissions
${ADMIN_USER} = ${ADMIN_PASSWORD}, root

# Additional users can be added here
# Example: admin = admin123, root

[roles]
# root role has all permissions
root = *:*
