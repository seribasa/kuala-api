# Kuala API Environment Variables Example
# Copy this file to .env and update the values for your environment

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================

# Supabase Project URL
# This is the base URL for your Supabase project
# Example: https://your-project-id.supabase.co
AUTH_BASE_URL=https://your-project-id.supabase.co

# Supabase Anonymous Key
# This is the public anonymous key for your Supabase project
# Found in: Supabase Dashboard > Settings > API > Project API keys > anon public
AUTH_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# =============================================================================
# EXTERNAL KEYCLOAK CONFIGURATION
# =============================================================================

# External Keycloak Configuration
# CONFIGURE these variables if using local development supabase project
# IGNORE and leave blank if using external supabase project and SETUP Keycloak there
# Enable or disable external Keycloak integration
EXTERNAL_KEYCLOAK_ENABLED=true
EXTERNAL_KEYCLOAK_CLIENT_ID=
EXTERNAL_KEYCLOAK_SECRET=
EXTERNAL_KEYCLOAK_REDIRECT_URI=
EXTERNAL_KEYCLOAK_URL=

# =============================================================================
# KILL BILL CONFIGURATION
# =============================================================================

# Kill Bill Server Configuration
# Base URL for your Kill Bill server instance
KILLBILL_BASE_URL=http://127.0.0.1:8080

# Kill Bill API Authentication
# API key and secret for Kill Bill API access base on tenant
# EXAMPLE VALUES (DO NOT USE IN PRODUCTION)
KILLBILL_API_KEY=demo 
KILLBILL_API_SECRET=demosecret

# Kill Bill Basic Authentication
# Username and password for Kill Bill admin access
# EXAMPLE VALUES (DO NOT USE IN PRODUCTION)
KILLBILL_USERNAME=admin
KILLBILL_PASSWORD=password

# =============================================================================
# ENTERPRISE CONTACT CONFIGURATION
# =============================================================================

# Enterprise Sales Contact Information
# Used for enterprise tier plans contact details
ENTERPRISE_CONTACT_EMAIL=
ENTERPRISE_CONTACT_PHONE=
ENTERPRISE_CONTACT_MESSAGE=

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

# Enable or disable CORS middleware
# true  = CORS handled by application (default)
# false = CORS handled by proxy/load balancer
CORS_ENABLED=true

# Allowed origins for CORS (comma-separated)
# "*" = Allow all origins (default, use only for development)
# Single origin example: http://localhost:4200
# Multiple origins example: http://localhost:4200,https://kuala-staging.seribasa.digital,https://kuala.seribasa.digital
CORS_ORIGIN=*

# DEVELOPMENT EXAMPLES:
# CORS_ENABLED=true
# CORS_ORIGIN=http://localhost:4200

# STAGING EXAMPLE:
# CORS_ENABLED=true
# CORS_ORIGIN=https://kuala-staging.seribasa.digital

# PRODUCTION EXAMPLE (Multiple domains):
# CORS_ENABLED=true
# CORS_ORIGIN=https://kuala.seribasa.digital,https://app.seribasa.digital

# PRODUCTION WITH PROXY/LB HANDLING CORS:
# CORS_ENABLED=false
# CORS_ORIGIN= (not used when disabled)